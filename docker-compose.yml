version: '3'

services:

  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
      
  db:
    image: mariadb
    restart: always
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=pwd